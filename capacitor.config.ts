import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'spotify.recs.anytime',
  appName: 'Spotify Discover Anytime',
  webDir: 'public/browser',
  bundledWebRuntime: false
};

export default config;
